/**
 * Configuración de GestionHorasTrabajo Chrome Extension
 * Este archivo será REEMPLAZADO cuando descargues el addon
 * con tu URL y token personalizados
 * 
 * Durante desarrollo, usará valores por defecto
 */

// Si estos no están definidos, los scripts usarán valores por defecto
if (typeof DEFAULT_APP_URL === 'undefined') {
  const DEFAULT_APP_URL = 'https://calendar.favala.es';
}

if (typeof EXTENSION_TOKEN === 'undefined') {
  const EXTENSION_TOKEN = '';  // Será reemplazado al descargar
}
